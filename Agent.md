# 🧠 Agent.md - Taima Al-Wani

## 📊 نظرة عامة
- **نوع المشروع:** تطبيق ويب كامل (Full-stack Web App) مع واجهة خادم خلفي على Cloudflare Pages Functions.
- **اللغة:** TypeScript / TSX
- **الإطار:** React + Vite + TailwindCSS v4 + Cloudflare Pages Functions (D1 Database)
- **نقطة الدخول:** `src/main.tsx` للواجهة الأمامية و `functions/[[route]].ts` للخادم الخلفي.

## 🌲 المخطط الشجري
taima-alwani/
├── functions/
│   └── [[route]].ts      # الخادم الخلفي ومسارات الـ API (Cloudflare Worker)
├── src/
│   ├── components/       # مكونات واجهة المستخدم (خريطة، لوحة إدارة، ألعاب)
│   ├── data/             # بيانات المحاكاة والقطاعات
│   ├── services/         # واجهات الاتصال مع الـ API والـ AI
│   ├── App.tsx           # المكون الرئيسي للتحكم في الحالة العامة
│   └── index.css         # ملف التنسيق العام
├── wrangler.toml         # تكوين Cloudflare D1 و Pages
├── package.json          # حزم وتبعيات المشروع
├── vite.config.ts        # إعدادات Vite للبناء
└── UserReport.md         # سجل التغييرات والتوثيق الموحد للمشروع

## 🛠️ أوامر التشغيل
| الأمر | الوظيفة |
|-------|---------|
| `npm run dev` | تشغيل بيئة التطوير المحلية |
| `npm run build` | بناء النسخة الإنتاجية والتحقق من الأنواع |
| `npm run lint` | تشغيل تدقيق الأنواع TypeScript |

## ✅ أفضل الممارسات المكتشفة
- **الهجرة الذاتية التلقائية:** فحص هيكلية قاعدة البيانات تلقائياً بطلب `PRAGMA table_info` وإجراء التعديلات (ALTER TABLE) لضمان اتساق قواعد بيانات D1 دون الحاجة لصلاحيات كتابة مباشرة من الـ API الخارجي.
- **التشفير الآمن المدمج:** تشفير كلمات المرور باستخدام Web Crypto API (خوارزمية PBKDF2) لضمان الأمان السيبراني المتوافق مع Edge Runtime في Cloudflare.
- **تأمين مسارات الإدارة:** التحقق من هيدر `X-Admin-Token` في مسارات خادم الإدارة لمنع الوصول غير المصرح به.
- **تنظيف الحزم البرمجية:** الحفاظ على نظافة كود TypeScript عن طريق إزالة تكرار الواجهات (Interfaces) مثل `Sector` واستيرادها من مصادرها الأصلية.

## ⚠️ المشاكل المعروفة والحلول
| المشكلة | السبب | الحل | التاريخ |
|---------|-------|------|---------|
| الإشعارات اليومية ثابتة (4 إشعارات فقط) وغير تفاعلية | المقولات والإشعارات مكتوبة بنص ثابت في المكونات بدون تتبع حالة القراءة | تنفيذ بنك إشعارات 50 إشعاراً متنوعاً، ودالة لاختيار 3 إشعارات يومية ديناميكية تعتمد على التاريخ كـ seed، مع دمج نظام read/unread مخزن في localStorage وتطبيقه في DashboardView و GlobalHeader وإخفاء النقطة الحمراء بعد القراءة. | 2026-05-26 |
| البريد الإلكتروني يسمح بتغييره ويجب منعه | إمكانية تعديل البريد من لوحة قيادة المستخدم في الإعدادات | استبدال خيار تعديل البريد بعنصر نصي دائم مع أيقونة قفل، وإلغاء حالات وState تعديل البريد من المكون، والتأكد من تفعيل الحماية الخلفية. | 2026-05-26 |
| زر تبديل المود (داكن/فاتح) في لعبة SSH لا يعمل | عدم دعم المود الفاتح بالكامل واعتماد اللعبة على ألوان تيرمينال ثابتة | إقرار المظهر الداكن الثابت كونه الأنسب واقعياً لطرفيات الاختراق السيبراني، مع إضافة مؤشر عائم 🖥️ Terminal Mode وتصفية المتغيرات غير المستخدمة. | 2026-05-26 |
| عدم كفاية الأسئلة الأمنية، غياب الرصيد، التلميح المجاني والإنذارات الصوتية في لعبة التشفير | وجود أسئلة عامة (أوامر Linux عادية)، وغياب المنطق البرمجي للرصيد والمؤثرات الصوتية والتحقق من التلميحات | تصفية الأسئلة، إدراج نظام رصيد (100 ابتدائي، +10/-5/-20)، تشغيل Oscillator صوتي عند الخطأ، إعداد كشف محمي للتلميحات، وإضافة شاشة نتائج Neobrutalism كاملة | 2026-05-26 |
| عدم وضوح عناصر اختبار تحديد المستوى والخطط في المود الداكن والفاتح | استخدام ألوان متغيرة Dynamic (bg-on-background/text-background) تنعكس بالوضع الفاتح | تعديل حاوية التنسيقات لتستخدم bg-background وتصحيح خيارات MCQ، وإضافة هيستوغرام فاخر وزر إعادة الاختبار. | 2026-05-26 |
| ألوان المليونير معكوسة وضبابية شاشة المساعدات والانسحاب | استخدام ألوان متغيرة Dynamic (bg-on-background/text-background أو primary) تنعكس بالوضع الفاتح | تثبيت اللون الأكاديمي الداكن (#447F98) بشكل دائم للعبة لمنع أي انعكاس، وتكبير أعمدة الجمهور وتأثير توهج المبلغ. | 2026-05-26 |
| خطأ `no such column: email` | عدم تحديث قاعدة البيانات عن بعد | تهيئة هجرة ذكية تلقائية داخل `onRequest` لتحديث الجداول ديناميكياً. | 2026-05-21 |
| تعذر نقر المقاطعات على الجوال | اعتراض التراكبات للماوس | تعديل Z-Index وإضافة `pointer-events-none` للتراكبات غير التفاعلية. | 2026-05-21 |

## 🧹 صيانة الذاكرة (Memory Hygiene)
- آخر تنظيف وصيانة: 2026-05-26
- **تم تنفيذ خطة نظام الإشعارات اليومية (Notifications System Plan 08) بالكامل وبنجاح تام**: تم التأكد من وجود بنك الإشعارات المكون من 50 إشعاراً في `notifications.ts` مع دالة الحصول على 3 إشعارات يومية ديناميكية متغيرة حسب التاريخ، وتم تحديث كل من `GlobalHeader.tsx` و `DashboardView.tsx` بالكامل لدمج البنك، وإضافة حالة تتبع القراءة `notificationsRead` المخزنة والمحدثة يومياً في `localStorage` لضمان إخفاء النقطة الحمراء الفعالة بمجرد نقر زر التنبيهات من قبل المستخدم وتزامن الحالتين، مع إخراج تصميم تفاعلي فاخر للإشعارات.
- تم تنفيذ خطة منع تغيير البريد الإلكتروني (Email Change Protection) بالكامل وبنجاح، حيث أُلغي خيار تعديل البريد الإلكتروني بالكامل من الواجهة الأمامية باستبدال زر التعديل بحاوية نصية أنيقة تعرض البريد الحالي بجانب أيقونة قفل `Lock` لتعريف المستخدم بعدم قابلية التغيير بصرياً، وتحديث الـ States والمنطق الداخلي للنموذج لإلغاء جميع حالات وقيم تعديل البريد، مع العلم أن الحماية الخلفية (Backend Defense) مطبقة ومحمية بالفعل في الخادم الخلفي Cloudflare Worker لمنع أي تعديل غير مصرح به على قاعدة البيانات.
- تم تنفيذ خطة إصلاح زر المود في لعبة اختراق الخادم (SSH Game Theme Fix) بالكامل وبنجاح، حيث تقرر الإبقاء على التصميم الداكن الكلاسيكي الثابت كخيار مثالي وواقعي لطرفيات الاختراق السيبراني، مع إضافة إشارة "🖥️ Terminal Mode" عائمة وتصفية الرموز والمتغيرات غير المستخدمة مثل `isTutorial` من واجهة المكون لزيادة تماسك ونظافة كود TypeScript.
- تم تنفيذ خطة إصلاح محاكاة المدينة الآمنة (Secure City) بالكامل بنجاح باهر، وتشمل: تطوير صفارة إنذار حقيقية (Siren) يتذبذب ترددها بين 800Hz و 400Hz وتعمل عند الأخطاء، وإضافة أزرار تلميحات مرئية في التيرمينال للمدافع مع خصم $1,000 وفي الألغاز للمدافع والمهاجم مع خصم $2,000 من الميزانية، وتحديث شروط الدخول للمدافع عند اختراق المقاطعات، وتطوير أوامر التيرمينال الجانبي (help, status, budget, level, attacks, scan, clear) مع فتح إدخال الأوامر بشكل دائم، وحل مشكلة نقر الدوائر المعطلة في المستويين 2 و 3 في `Map.tsx` تماماً بإلغاء `SectorClickHandler` وجعل `CircleMarker` تفاعلية مباشرة، وإظهار الرصيد بشكل دائم في الهيدر، وإضافة أزرار العودة السريعة في جميع شاشات النهاية.
- تم تنفيذ خطة إصلاح لعبة تشفير الأوامر (Crypto Puzzles) بالكامل بنجاح تام، وتشمل تصفية الألغاز، نظام الرصيد, حماية التلميحات بخصم النقاط، إضافة صوت الإنذار Oscillator عند الخطأ، مسح الخانات تلقائياً، وبطاقة النتائج النهائية.
- تم تنفيذ خطة نظام تقدم الألعاب وقفل/فتح المستويات (Game Progression & Locking System) بالكامل وبنجاح باهر، وتشمل: إعداد قواعد الفتح `GAME_UNLOCK_RULES` في ملف `gameProgression.ts` وقفل ألغاز التشفير (يتطلب التقييم)، والمليونير (يتطلب التقييم والتشفير)، والمدينة الآمنة (يتطلب التقييم والتشفير والمليونير)، وSSH (يتطلب القطاع الأساسي للمدينة)، مع استثناء حساب المدير (admin) ليرى الكل مفتوحاً بالكامل. تم إعداد مستويي "البنية التحتية" و"الحالة الحرجة" ليعتمدا على إنجازات المستخدم السابقة للمستويات الأدنى. تم تصميم أسلوب القفل البصري بتصفية رمادية (grayscale) وأيقونة قفل (Lock) وكرت إرشادي يعرض شروط الفتح، وتفعيل اتصال وتمرير الحالة لجميع مسارات الألعاب واستدعاء حفظ الإنجازات والتقدم تلقائياً إلى قاعدة البيانات D1.
- تم التحقق من نجاح البناء `npm run build` محلياً بنجاح كامل وبدون أي خطأ أو تحذير في TypeScript.
