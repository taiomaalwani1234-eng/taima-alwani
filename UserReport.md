# 📋 UserReport - Taima Al-Wani Cyber Academy

## 🏗️ نظرة عامة
تطبيق "أكاديمية تيماء العلواني للأمن السيبراني" هو منصة تعليمية تفاعلية تحاكي الهجمات السيبرانية وتقدم ألعاباً تعليمية (مثل محاكاة المدينة الآمنة Secure City، ولعبة من سيربح المليونير السيبرانية، ومسابقات التشفير).
يعتمد التطبيق على واجهة أمامية مبنية بـ Vite + React وتنسيقات TailwindCSS v4، وخادم خلفي بدون خادم (Serverless) يعمل على Cloudflare Pages Functions وقاعدة بيانات Cloudflare D1 (SQLite).

---

## 📝 سجل التغييرات
| التاريخ | التغيير | الملفات المتأثرة |
|---------|---------|------------------|
| 2026-05-21 | إزالة 14 ملفاً مؤقتاً ومهاجراً كانت مبعثرة في جذر المشروع. | جذر المشروع |
| 2026-05-21 | إدراج آلية الهجرة الذاتية التلقائية لقاعدة البيانات (PRAGMA table_info) للتأكد من وجود عمودي `email` و `password_hash` وحل خطأ D1 المفقود. | `functions/[[route]].ts` |
| 2026-05-21 | تشفير كلمات المرور باستخدام خوارزمية PBKDF2 والـ Web Crypto API لحماية الحسابات مع دعم الترقية التلقائية لكلمات المرور القديمة المكتوبة بنص عادي عند أول تسجيل دخول ناجح. | `functions/[[route]].ts` |
| 2026-05-21 | تأمين مسارات الإدارة (Admin Endpoints) مثل الحصول على قائمة المستخدمين وحذفهم وسجلات النظام بطلب هيدر `X-Admin-Token` ومطابقته للسر البيئي `ADMIN_SECRET`. | `functions/[[route]].ts`, `src/components/AdminView.tsx` |
| 2026-05-21 | حجب إرجاع عمود `password_hash` نهائياً من أي استعلام يرجع بيانات المستخدمين لضمان عدم تسريب البيانات الحساسة. | `functions/[[route]].ts` |
| 2026-05-21 | إصلاح مشكلة عدم الاستجابة للنقرات على خريطة المقاطعات عبر سحب الـ `pointer-events-auto` من الطبقات الهيكلية الفارغة وتعديل الـ Z-index والتراكبات. | `src/components/SecureCityView.tsx` |
| 2026-05-21 | إضافة زر عودة عائم وبارز أسفل الخريطة في الجوال والكمبيوتر يسهل العودة للوحة القيادة مستعيناً بأيقونة `ArrowRight` من lucide-react. | `src/components/SecureCityView.tsx` |
| 2026-05-21 | حل مشكلة تعليق شريط التمرير الجانبي (Operations Guidance Mobile Sidebar) على شاشات الجوال باستخدام `h-[calc(100dvh-64px)]` وتفعيل `overscroll-contain` وسرعة التمرير الأصلية في iOS. | `src/components/SecureCityView.tsx` |
| 2026-05-21 | إزالة تكرار واجهة `Sector` من `Map.tsx` واستيرادها مباشرة من `src/data/cityData.ts` لضمان نظافة وتماسك كود TypeScript. | `src/components/Map.tsx` |
| 2026-05-21 | تحسين الأداء على الهواتف المحمولة بإخفاء تراكب مسح CRT Scanning Overlay وتقليل تصفية Backdrop-blur للوحات الزجاجية إلى 4px لرفع معدل الإطارات (FPS). | `src/components/SecureCityView.tsx`, `src/index.css` |
| 2026-05-21 | إزالة تضمين مفتاح البيئة الحساس `process.env.GEMINI_API_KEY` من حزمة الواجهة الأمامية في إعدادات Vite لمنع كشفه في المتصفح. | `vite.config.ts` |
| 2026-05-21 | تطوير لعبة "اختراق الخادم" — محاكاة طرفية SSH كاملة مع 5 مراحل: إحاطة عسكرية، اتصال SSH، إدخال كلمة السر، استكشاف الخادم بأوامر Linux حقيقية، ونظام حظر fail2ban. تتضمن نظام نقاط وتقييم. | `src/components/SSHGameView.tsx`, `src/data/sshGameData.ts`, `src/App.tsx`, `src/components/DashboardView.tsx` |
| 2026-05-21 | دفع التغيرات ولعبة محاكاة اختراق SSH والتحديثات الأخيرة بنجاح إلى مستودع GitHub. | جميع ملفات المشروع |


---

## 🐛 المشاكل والحلول
| المشكلة | الحالة | الحل |
|---------|--------|------|
| `D1_ERROR: no such column: email` | **محلولة** | إضافة دالة `initDB` تقوم بالتحقق من هيكلية الجداول باستخدام `PRAGMA table_info` وإجراء `ALTER TABLE` ديناميكياً لإضافة أعمدة البريد الإلكتروني والهاش بشكل آمن. |
| تعذر نقر مقاطعات المدينة على الجوال | **محلولة** | نقل وتعديل تراكب الحقول وتطبيق `pointer-events-none` على الأعمدة الفارغة لمنع اعتراض النقرات. |
| كلمات المرور المخزنة بنص عادي | **محلولة** | تشفيرها تلقائياً باستخدام PBKDF2 والـ Web Crypto API المدمج في بيئة Cloudflare Workers مع ترقية تلقائية عند أول تسجيل دخول. |
| كشف مفتاح Gemini في حزمة الواجهة الأمامية | **محلولة** | إزالة كود تعريف المتغير في `vite.config.ts` والتحول لاستخدام خادم Cloudflare كوكيل للطلبات. |

---

## 💻 أخطاء التيرمينال
| الأمر | الخطأ | الحل |
|-------|-------|------|
| `rm -f fix-bells.js ...` | `Remove-Item: Parameter cannot be processed because...` | نظام التشغيل استخدم PowerShell كـ shell افتراضي حيث تتعارض المعلمة `-f` مع دالة الحذف. تم تبديل الأمر إلى صيغة PowerShell القياسية: `Remove-Item -Force ...` بنجاح. |
| `npm run build` | `'vite' is not recognized as an internal or external command` | حزم `node_modules` لم تكن مثبتة محلياً في المجلد الجديد. تم تشغيل `npm install` أولاً بنجاح، ثم أعيد البناء بنجاح تام. |
